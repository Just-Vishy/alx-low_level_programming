#include "main.h"
#include <ctype.h>

/**
 * _islower - Entry point
 *
 * @c: is the character
 *
 * _putchar - prints out characters
 */

int _islower(int c)
{
	if (_islower(c))
	{
		return(1);
	}
	else {
		return(0);
	}
}
